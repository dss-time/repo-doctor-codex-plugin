# Examples
User: "Audit config precedence and environment drift; report names and locations only."
Expected: map sources and consumers, compare evidence-backed environments, and never expose values.
Non-trigger: "Run a broad repository health check." Use `project-health-check`.
