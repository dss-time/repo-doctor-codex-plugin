# Examples
User: "Review this column backfill and constraint migration for zero-downtime rollout; do not run it."
Expected: separate schema/data/app phases and assess locks, compatibility, verification, and rollback.
Non-trigger: "Create the implementation plan for an already reviewed change." Use `safe-change-plan`.
