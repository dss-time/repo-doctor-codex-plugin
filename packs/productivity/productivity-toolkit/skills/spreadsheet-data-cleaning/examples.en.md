# Examples

- “Profile this CSV, normalize dates and booleans, flag ambiguous rows, and save a new cleaned file with an audit log.”
- “Deduplicate this workbook using the supplied business key, but preserve every uncertain record.”
